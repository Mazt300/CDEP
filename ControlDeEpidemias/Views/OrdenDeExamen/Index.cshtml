@model IEnumerable<ControlDeEpidemias.Models.OrdenDeExamen>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.fechaorden)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.estado)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Medico.idmedico)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.paciente.nombre)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.fechaorden)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.estado)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Medico.idmedico)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.paciente.nombre)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idOrdenDeExamen }) |
            @Html.ActionLink("Details", "Details", new { id=item.idOrdenDeExamen }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idOrdenDeExamen })
        </td>
    </tr>
}

</table>
